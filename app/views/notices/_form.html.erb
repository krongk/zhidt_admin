<%= simple_form_for(@notice) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.input :level %>
    <%= f.input :message %>
    <%= f.input :is_read %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
