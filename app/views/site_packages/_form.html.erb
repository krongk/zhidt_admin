<%= simple_form_for(@site_package) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :site %>
    <%= f.association :package %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
